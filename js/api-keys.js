/**
 * SECURITY NOTICE:
 * API keys are now stored server-side in .env and proxied through /api/chat.
 * This file is kept as a placeholder — do NOT add real API keys here.
 * The frontend never needs direct API access.
 */
window.SECUREWEALTH_KEYS = {
  // Keys are now managed securely on the backend.
  // See server.js and .env.example for configuration.
};
